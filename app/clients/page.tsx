"use client";
import { useEffect, useState } from "react";
import { clients as demoClients } from "../../lib/demo-data";
import { ClientCard } from "../../components/client-card";
import { Shell } from "../../components/shell";
import { createClient, fetchClients, updateClient } from "../../lib/supabase-data";
import { ClientDetail } from "../../lib/types";

export default function ClientsPage() { const [clients, setClients] = useState<ClientDetail[]>(demoClients); const [message, setMessage] = useState("Loading connected clients…"); const [showForm, setShowForm] = useState(false); const [editing, setEditing] = useState<ClientDetail | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); useEffect(() => { fetchClients().then((rows) => { setClients(rows); setMessage(rows.length ? "Connected to Supabase" : "No clients added yet"); }).catch(() => setMessage("Showing demo clients until Supabase access is configured.")); }, []); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(""); const form = new FormData(event.currentTarget); try { const input = { name: String(form.get("name")), industry: String(form.get("industry")), location: String(form.get("location")), website: String(form.get("website")), email: String(form.get("email")), phone: String(form.get("phone")), health_score: Number(form.get("health_score") || 0) }; if (editing) { await updateClient(editing.id, input); setMessage("Client updated successfully"); } else { await createClient(input); setMessage("Client added successfully"); } setShowForm(false); setEditing(null); setClients(await fetchClients()); } catch (e) { setError(e instanceof Error ? e.message : "Unable to save client."); } finally { setBusy(false); } } const openEdit = (client: ClientDetail) => { setEditing(client); setShowForm(true); setError(""); }; return <Shell active="Clients"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>Clients</h1><p className="lede">A clear, current view of every account you manage.</p><small className="updated">{message}</small></div><button className="button button-dark" onClick={() => { setEditing(null); setShowForm(!showForm); }}>+ Add client</button></div>{showForm && <form className="detail-card client-form" onSubmit={submit}><p className="eyebrow">{editing ? "Update account" : "New account"}</p><h2>{editing ? `Edit ${editing.name}` : "Add a client"}</h2><div className="form-grid"><input name="name" defaultValue={editing?.name} placeholder="Business name" required /><input name="industry" defaultValue={editing?.industry} placeholder="Industry" required /><input name="email" type="email" defaultValue={editing?.email} placeholder="Contact email" required /><input name="phone" type="tel" defaultValue={editing?.phone} placeholder="Phone number" required /><input name="location" defaultValue={editing?.location} placeholder="Location" /><input name="website" defaultValue={editing?.website} placeholder="Website" /><input name="health_score" type="number" min="0" max="100" defaultValue={editing?.health} placeholder="Health score" /></div>{error && <p className="login-notice error">{error}</p>}<button className="button button-dark" disabled={busy}>{busy ? "Saving…" : editing ? "Save changes" : "Save client"}</button></form>}<section className="client-grid client-grid-wide">{clients.map((client) => <div key={client.id}><ClientCard client={client} /><button className="text-link" onClick={() => openEdit(client)}>Edit client →</button></div>)}</section></Shell>; }
