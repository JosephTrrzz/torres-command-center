"use client";
import Link from "next/link";
import { useState } from "react";
const nav = [{label:"Overview",href:"/"},{label:"Clients",href:"/clients"},{label:"Reports",href:"#"},{label:"Settings",href:"#"}];
export function Shell({children,active}:{children:React.ReactNode;active:string}) { const [open,setOpen]=useState(false); const logout=()=>{window.localStorage.removeItem("torres-demo-session");window.location.href="/login/"}; return <div className="app-shell"><aside className={`sidebar ${open?"open":""}`}><div className="brand"><span className="brand-mark">T</span><span>Torres <i>& Co.</i></span></div><div className="workspace"><span className="workspace-avatar">TC</span><div><small>Workspace</small><strong>Torres & Co. <b>⌄</b></strong></div></div><nav aria-label="Main navigation">{nav.map((item)=><Link className={active===item.label?"active":""} href={item.href} key={item.label}><span className="nav-icon">{item.label === "Overview" ? "◈" : item.label === "Clients" ? "◎" : item.label === "Reports" ? "▤" : "⚙"}</span>{item.label}{item.label === "Clients" && <em>3</em>}</Link>)}</nav><div className="sidebar-bottom"><div className="profile"><span className="avatar">JT</span><div><strong>Joseph Torres</strong><small>Admin</small></div><button className="logout-button" onClick={logout}>Log out</button></div></div></aside><main className="main"><header className="mobile-header"><button onClick={()=>setOpen(!open)} aria-label="Toggle menu">☰</button><span className="brand-mark">T</span><strong>Torres & Co.</strong><button className="mobile-logout" onClick={logout}>Log out</button></header><div className="topbar"><span className="breadcrumb">Torres & Co. <b>/</b> {active}</span><div className="top-actions"><button className="round-button" aria-label="Notifications">♢<span /></button><button className="top-avatar" onClick={logout} aria-label="Log out">JT</button></div></div><div className="content">{children}</div></main></div>; }
